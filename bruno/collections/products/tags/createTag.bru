meta {
  name: createTag
  type: http
  seq: 5
}

post {
  url: {{URL}}/api/tags
  body: json
  auth: inherit
}

body:json {
  {
    "name": "hello"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
