services:
  mongo:
    image: mongo
    restart: always
    container_name: mongo
    command: mongod --replSet rs0 --bind_ip_all --noauth
    ports:
      - 27017:27017
    volumes:
      - ./mongo-data:/data/db
    networks:
      - mongo-network

  init-mongo:
    image: mongo
    container_name: init-mongo
    depends_on:
      - mongo
    entrypoint: >
      bash -c "
        sleep 5

        if mongosh --host mongo --quiet --eval \"rs.status()\" 2>&1 | grep -q \"no replset config\"; then
          echo \"Initializing replica set...\"
          mongosh --host mongo --eval \"rs.initiate({
            _id: 'rs0',
            members: [{ _id: 0, host: 'mongo:27017' }]
          })\"
          echo \"Replica set initialized.\"
        else
          echo \"Replica set already initialized.\"
        fi
        "
    restart: "no"
    networks:
      - mongo-network

  # mongo-express:
  #   image: mongo-express
  #   restart: always
  #   ports:
  #     - 8081:8081
  #   environment:
  #     ME_CONFIG_MONGODB_URL: mongodb://mongo?replicaSet=rs0&retryWrites=true&directConnection=true
  #     ME_CONFIG_BASICAUTH_ENABLED: false
  #   depends_on:
  #     - mongo
  #   networks:
  #     - mongo-network

networks:
  mongo-network:
    driver: bridge
